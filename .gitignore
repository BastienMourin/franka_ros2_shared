.env
.idea
.vscode
*.pyc
COLCON_IGNORE
AMENT_IGNORE
cmake-build-*/
build/
install/
log/

# Pulled in by `vcs import src < src/franka.repos`, not tracked here.
libfranka
franka_description

# Cloned separately, not tracked here. Required by
# my_cartesian_impedance_controller_cmp_dq -- see that package's README.
dq_operations/

# Companion DQ/DMP packages, if you check them out into the same workspace.
# See https://github.com/BastienMourin/dq_dmp_ros2_shared
dq_control/
dq_controller_tools/
dual_quaternions/
ros_dq_operations/

# Logs and plots produced by scripts/*.py. Regenerated by re-running them.
*.npz
*.png
